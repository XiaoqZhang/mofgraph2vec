defaults:
  - _self_
  - mof2vec_data: 
    - data.yaml
  - mof2vec_model:
    - gensim.yaml
  - doc2label_data: 
    - rsm.yaml 
  - doc2label_model:
    - ensemble.yaml
  - trainer:
    - default.yaml
  - sweep:
    - workflow.yaml

logger:
  project: mof2vec
  entity: xiaoqiz
  mode: offline
  tags: 

seed: 5432
sweep: false
mode: doc2label # available choices: mof2vec, doc2label, workflow